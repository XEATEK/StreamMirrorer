services:
  streammirrorer:
    image: ${DOCKER_REGISTRY-}streammirrorer
    build:
      context: .
      dockerfile: StreamMirrorer/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ./Streams:/app/Streams
